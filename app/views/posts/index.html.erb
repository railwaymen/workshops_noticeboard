<% title link_to("Posts", posts_path) %>

<% for post in @posts %>
  <div>
    <h3><%= post.title %></h3>
    <p><%= post.content %></p>
    <p><%= link_to "Show", post %></p>
  </div>
  <hr />
<% end %>
